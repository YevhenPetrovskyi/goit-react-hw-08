import { Box, LinearProgress } from '@mui/material';

const SectionLoader = () => {
  return (
    <Box sx={{ width: '100%', mt: 2 }}>
      <LinearProgress />
    </Box>
  );
};

export default SectionLoader;
